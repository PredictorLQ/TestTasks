# Portal API

RESTful API для управления задачами, построенное на ASP.NET Core 9 с использованием Clean Architecture, CQRS и других современных подходов.

## 🛠 Технологии

### Основной стек
- **.NET 9.0** - Платформа разработки
- **ASP.NET Core 9** - Веб-фреймворк для создания API
- **PostgreSQL** - Реляционная база данных
- **Entity Framework Core 9.0** - ORM для работы с базой данных (Code-First подход)

### Библиотеки и инструменты
- **MediatR 12.2.0** - Реализация паттерна Mediator для CQRS
- **FluentValidation 11.9.0** - Валидация входных данных
- **Mapster 7.4.0** - Высокопроизводительный маппинг объектов
- **Swashbuckle.AspNetCore 6.5.0** - Генерация Swagger/OpenAPI документации
- **Npgsql.EntityFrameworkCore.PostgreSQL 9.0.2** - Провайдер PostgreSQL для EF Core

## 🏗 Архитектурные подходы

### Clean Architecture (Чистая архитектура)
Проект организован в виде многослойной архитектуры с четким разделением ответственности:

```
Portal.Domain          - Доменный слой (сущности, DTO, модели, enum)
Portal.Application     - Слой приложения (команды, запросы, контракты)
Portal.Infrastructure  - Слой инфраструктуры (репозитории, сервисы, EF Core)
Portal.Api             - Слой представления (контроллеры, bindings, настройки)
```

**Принципы:**
- Зависимости направлены внутрь (от внешних слоев к внутренним)
- Доменный слой не зависит от других слоев
- Слои взаимодействуют через интерфейсы (Dependency Inversion)

### CQRS (Command Query Responsibility Segregation)
Разделение операций на команды (изменение данных) и запросы (чтение данных):

- **Commands** - операции изменения данных (Create, Update, Delete)
  - `CreateTaskCommand`, `UpdateTaskCommand`, `DeleteTaskCommand`
  - `CreateTaskTypeCommand`, `UpdateTaskTypeCommand`, `DeleteTaskTypeCommand`

- **Queries** - операции чтения данных (Get, GetAll)
  - `GetAllTasksQuery`, `GetTaskByIdQuery`
  - `GetAllTaskTypesQuery`, `GetTaskTypeByIdQuery`

### MediatR Pattern
Использование паттерна Mediator для обработки команд и запросов:
- Централизованная обработка запросов
- Pipeline behaviors для валидации и логирования
- Упрощение зависимостей между компонентами

### Repository Pattern
Абстракция доступа к данным через репозитории:
- `ITaskRepository`, `ITaskTypeRepository` - интерфейсы
- `TaskRepository`, `TaskTypeRepository` - реализации

### Service Layer
Бизнес-логика вынесена в сервисы:
- `ITaskService`, `ITaskTypeService` - интерфейсы
- `TaskService`, `TaskTypeService` - реализации

### Validation
Валидация входных данных через FluentValidation:
- Валидаторы для каждой команды
- Автоматическая валидация через Pipeline Behavior
- Детальные сообщения об ошибках

### Object Mapping
Использование Mapster для маппинга объектов:
- Высокая производительность
- Настраиваемые маппинги
- Уменьшение boilerplate кода

### Soft Delete
Мягкое удаление записей:
- Флаг `IsDeleted` вместо физического удаления
- Сохранение истории данных
- Возможность восстановления

## 📁 Структура проекта

```
Portal/
├── Portal.Domain/                    # Доменный слой
│   ├── Entities/                    # Сущности домена
│   │   ├── Entity.cs               # Базовый класс сущности
│   │   ├── Task.cs                 # Сущность задачи
│   │   └── TaskType.cs             # Сущность типа задачи
│   ├── Dtos/                        # Data Transfer Objects
│   │   ├── TaskDto.cs
│   │   └── TaskTypeDto.cs
│   ├── Models/                      # Модели для операций
│   │   ├── TaskModel.cs
│   │   └── TaskTypeModel.cs
│   └── Enums/                       # Перечисления
│       ├── TaskStatusEnum.cs
│       └── TaskPriorityEnum.cs
│
├── Portal.Application/              # Слой приложения
│   ├── Commands/                    # Команды (CQRS)
│   │   ├── Tasks/
│   │   └── TaskTypes/
│   ├── Queries/                     # Запросы (CQRS)
│   │   ├── Tasks/
│   │   └── TaskTypes/
│   ├── Contracts/                   # Контракты (интерфейсы)
│   │   ├── Repositories/
│   │   └── Services/
│   ├── Behaviors/                   # Pipeline behaviors
│   │   └── ValidationBehavior.cs
│   └── Extensions/
│       └── DependencyInjectionExtensions.cs
│
├── Portal.Infrastructure/           # Слой инфраструктуры
│   ├── Data/                        # Работа с данными
│   │   ├── PortalDbContext.cs      # Контекст EF Core
│   │   ├── Configurations/         # Конфигурации сущностей
│   │   ├── Migrations/             # Миграции базы данных
│   │   └── SeedData.cs             # Начальные данные
│   ├── Repositories/               # Реализации репозиториев
│   ├── Services/                    # Реализации сервисов
│   ├── Mappings/                    # Настройки Mapster
│   └── Extensions/
│       └── DependencyInjectionExtensions.cs
│
└── Portal.Api/                      # Слой представления
    ├── Controllers/                 # API контроллеры
    │   ├── TasksController.cs
    │   └── TaskTypesController.cs
    ├── Bindings/                    # Модели для API
    ├── Extensions/                  # Расширения
    │   └── SwaggerExtensions.cs
    ├── Program.cs                   # Точка входа
    └── appsettings.json            # Конфигурация
```

## 🚀 Запуск проекта

### Требования
- **.NET 9.0 SDK** - [Скачать](https://dotnet.microsoft.com/download/dotnet/9.0)
- **PostgreSQL 12+** - [Скачать](https://www.postgresql.org/download/)
- **Visual Studio 2022** или **JetBrains Rider** или **VS Code** (опционально)

### Настройка базы данных

1. Установите и запустите PostgreSQL
2. Создайте базу данных (опционально, создастся автоматически):
```sql
CREATE DATABASE portal_db;
```

3. Настройте строку подключения в `Portal.Api/appsettings.json`:
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=portal_db;Username=postgres;Password=root"
  }
}
```

### Запуск через Visual Studio / Rider

1. Откройте решение `Portal.sln`
2. Установите `Portal.Api` как стартовый проект
3. Нажмите F5 или Run

### Запуск через командную строку

1. Перейдите в директорию проекта:
```bash
cd "D:\projects\TestTasks\c#\Портал\Portal.Api"
```

2. Восстановите зависимости:
```bash
dotnet restore
```

3. Примените миграции (выполняется автоматически при запуске):
```bash
dotnet ef database update --project ..\Portal.Infrastructure
```

4. Запустите проект:
```bash
dotnet run
```

5. Откройте браузер и перейдите по адресу:
```
https://localhost:5001
```
или
```
http://localhost:5000
```

### Swagger UI

После запуска проекта Swagger UI доступен по адресу:
```
https://localhost:5001
```
или
```
http://localhost:5000
```

Swagger UI предоставляет:
- Интерактивную документацию API
- Возможность тестирования всех эндпоинтов
- Описание моделей и схем данных
- Примеры запросов и ответов

## 📡 API Эндпоинты

### Задачи (Tasks)

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/Tasks` | Получить все задачи |
| GET | `/api/Tasks/{id}` | Получить задачу по ID |
| POST | `/api/Tasks` | Создать новую задачу |
| PUT | `/api/Tasks/{id}` | Обновить задачу |
| DELETE | `/api/Tasks/{id}` | Удалить задачу (мягкое удаление) |

### Типы задач (TaskTypes)

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/TaskTypes` | Получить все типы задач |
| GET | `/api/TaskTypes/{id}` | Получить тип задачи по ID |
| POST | `/api/TaskTypes` | Создать новый тип задачи |
| PUT | `/api/TaskTypes/{id}` | Обновить тип задачи |
| DELETE | `/api/TaskTypes/{id}` | Удалить тип задачи (мягкое удаление) |

## 📝 Примеры использования

### Создание задачи

```http
POST /api/Tasks
Content-Type: application/json

{
  "title": "Реализовать новую функцию",
  "description": "Добавить возможность экспорта данных",
  "status": "Pending",
  "priority": "High",
  "dueDate": "2024-12-31T23:59:59Z",
  "taskTypeId": "3fa85f64-5717-4562-b3fc-2c963f66afa6"
}
```

### Создание типа задачи

```http
POST /api/TaskTypes
Content-Type: application/json

{
  "name": "Разработка",
  "description": "Задачи, связанные с разработкой программного обеспечения"
}
```

### Получение всех задач

```http
GET /api/Tasks
```

### Обновление задачи

```http
PUT /api/Tasks/3fa85f64-5717-4562-b3fc-2c963f66afa6
Content-Type: application/json

{
  "title": "Обновленное название задачи",
  "description": "Обновленное описание",
  "status": "InProgress",
  "priority": "Medium",
  "dueDate": "2024-12-25T23:59:59Z",
  "taskTypeId": "3fa85f64-5717-4562-b3fc-2c963f66afa6"
}
```

## 🔧 Настройки и конфигурация

### Логирование

Уровни логирования настраиваются в `appsettings.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Information"
    }
  }
}
```

### Миграции

Миграции применяются автоматически при запуске приложения. Для создания новой миграции:

```bash
dotnet ef migrations add MigrationName --project Portal.Infrastructure --startup-project Portal.Api
```

### Начальные данные

При первом запуске автоматически заполняются типы задач:
- Разработка
- Тестирование
- Документация
- Исправление ошибок
- Рефакторинг

## 🎯 Особенности реализации

### Primary Constructors
Использование Primary Constructors (C# 12) для упрощения кода:
```csharp
public sealed class TaskService(
    ITaskRepository taskRepository,
    ILogger<TaskService> logger) : ITaskService
```

### Record Types
Использование record для DTO:
```csharp
public sealed record TaskDto(
    Guid Id,
    string Title,
    // ...
);
```

### Enum как строки
Enum сериализуются как строки в JSON для лучшей читаемости:
```csharp
options.JsonSerializerOptions.Converters.Add(
    new JsonStringEnumConverter()
);
```

### XML Documentation
Подробная XML документация для всех публичных API методов с описаниями параметров, ответов и примерами.

## 📦 Зависимости проекта

```
Portal.Api
  ├── Portal.Application
  └── Portal.Infrastructure

Portal.Application
  └── Portal.Domain

Portal.Infrastructure
  ├── Portal.Domain
  └── Portal.Application
```

## 🧪 Тестирование API

Используйте Swagger UI для тестирования API:
1. Откройте Swagger UI в браузере
2. Выберите нужный эндпоинт
3. Нажмите "Try it out"
4. Заполните параметры
5. Нажмите "Execute"

## 📚 Дополнительная информация

### Статусы задач
- `Pending` - Ожидает выполнения
- `InProgress` - В процессе выполнения
- `Completed` - Выполнена
- `Cancelled` - Отменена

### Приоритеты задач
- `Low` - Низкий приоритет
- `Medium` - Средний приоритет
- `High` - Высокий приоритет
- `Critical` - Критический приоритет

## 🔒 Безопасность

- Валидация всех входных данных через FluentValidation
- Защита от SQL-инъекций через параметризованные запросы EF Core
- Мягкое удаление для сохранения истории данных

## 📄 Лицензия

Проект создан в образовательных целях.

## 👨‍💻 Разработка

Проект следует принципам:
- **SOLID** - Принципы объектно-ориентированного программирования
- **DRY** - Don't Repeat Yourself
- **KISS** - Keep It Simple, Stupid
- **YAGNI** - You Aren't Gonna Need It

---

**Версия:** 1.0.0  
**Платформа:** .NET 9.0  
**База данных:** PostgreSQL 12+

